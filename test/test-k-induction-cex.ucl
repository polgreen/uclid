module main
{
  var x, y : integer;
  init {
    x = 0;
    y = 1;
  }
  procedure do_something()
    modifies x, y;
  {
    {
      var z : integer;
      z = 1;
      x, y = x+y, y+z;
    }
  }
  
  next {
    call do_something();
  }
  
  invariant x_le_y: x <= y;
  
  control {
    v = induction(4);
    check;
    print_results;
    v.print_cex(x, y);
  }
}

